# El proxy exterior recibe las imágenes antes que el Nginx del frontend.
# Se deja un pequeño margen sobre los 10 MB anunciados por los formularios
# para la codificación multipart/form-data.
client_max_body_size 15m;
